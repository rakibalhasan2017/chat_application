export const signup = (req, res) => {
    res.status(201).send("signup route");
}

export const login = (req, res) => {
    res.status(201).send("login route");
}

export const logout = (req, res) => {
    res.status(201).send("logout route");
}